package pages;

import constants.IConstants;
import org.openqa.selenium.WebDriver;

public class HomePage extends BasePage implements IConstants {

    public HomePage(WebDriver driver) {
        super(driver);
    }


}
